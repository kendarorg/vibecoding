Added on the switchView

for tree

                document.querySelector('.view-buttons').classList.remove('active');
for images
                document.querySelector('.view-buttons').classList.toggle('active');

removed uuid_created and used the correct function